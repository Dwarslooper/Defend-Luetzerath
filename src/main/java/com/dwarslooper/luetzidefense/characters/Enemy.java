package com.dwarslooper.luetzidefense.characters;

import org.bukkit.entity.Entity;

public abstract class Enemy extends Character {
    public Enemy(String displayName, int id, int level) {
        super(displayName, id, level);
    }
}
